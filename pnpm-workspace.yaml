packages:
  - packages/*
# pnpm 12 refuses to run dependency build scripts that are not listed here (fatal in CI / Docker).
# esbuild's postinstall only checks its binary; bufferutil and utf-8-validate are optional ws accelerators.
allowBuilds:
  esbuild: true
  bufferutil: false
  utf-8-validate: false
